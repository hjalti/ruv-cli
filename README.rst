A command line interface for watching RUV
=========================================

Installation
------------

Run

    pip install git+git://github.com/celery/django-celery.git

and then `ruv --help`.
